# CI bootstrap verification

This docs-only change exists to trigger the first pull-request CI against the consolidated v0.9 storefront source. It must not be merged; after verification it can be closed.
