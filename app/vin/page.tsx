import type { Metadata } from "next";
import Link from "next/link";
import { VehicleIdentityResolver } from "@/components/VehicleIdentityResolver";
export const metadata: Metadata = { title: "Визначення авто за номером або VIN", description: "Визначте автомобіль за державним номером або VIN і використовуйте один privacy-safe Vehicle Context у магазині та на СТО TURBO LEV." };
export default function VinPage(){return <div className="shell page section narrow"><div className="breadcrumbs"><Link href="/">Головна</Link><span>›</span><span>Авто: номер / VIN</span></div><span className="kicker">TURBO LEV · VEHICLE IDENTITY</span><h1>ОДНЕ АВТО ДЛЯ МАГАЗИНУ ТА СТО</h1><p className="lead">Введіть державний номер або VIN. Браузер передає ідентифікатор тільки same-origin Public BFF; у persistent UI context зберігаються лише маска та безпечні vehicle facts.</p><VehicleIdentityResolver title="ЗНАЙДЕМО ВАШЕ АВТО" purpose="GLOBAL"/><div className="steps"><article><b>1</b><strong>Номер або VIN</strong><p>Система сама визначає тип і нормалізує ввід.</p></article><article><b>2</b><strong>Server-side resolution</strong><p>Public BFF → OIDC Integration API. CRM DB не доступна браузеру.</p></article><article><b>3</b><strong>Один Vehicle Context</strong><p>Каталог, AI, кошик і booking використовують однаковий opaque context.</p></article></div></div>}
